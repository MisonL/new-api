{
  "name": "new-api-tauri-desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:version": "node ./scripts/sync-version.mjs",
    "check:version": "node ./scripts/check-version-sync.mjs",
    "prepare:web": "node ./scripts/build-web.mjs",
    "prepare:sidecar": "node ./scripts/prepare-sidecar.mjs",
    "test:rust": "cargo test --manifest-path ./src-tauri/Cargo.toml --test runtime_support",
    "check:rust": "cargo check --manifest-path ./src-tauri/Cargo.toml",
    "smoke": "node ./scripts/smoke.mjs",
    "dev": "bun run prepare:version && bun run prepare:web && bun run prepare:sidecar && bunx @tauri-apps/cli@2.10.1 dev --config ./src-tauri/tauri.conf.json",
    "build": "bun run prepare:version && bun run prepare:web && bun run prepare:sidecar && bunx @tauri-apps/cli@2.10.1 build --config ./src-tauri/tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.10.1"
  }
}
